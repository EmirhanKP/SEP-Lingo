import { Routes } from '@angular/router';
import { SnakeGameComponent } from './snake-game/snake-game.component'; // Importiere die Komponente

export const routes: Routes = [
  {
    path: 'snake-game',
    component: SnakeGameComponent
  }
];
